# 셸 스크립트는 항상 LF 유지 (CRLF 시 Git Bash에서 '\r' 파싱 오류로 hook 실패)
*.sh text eol=lf
# bash가 source하는 profile도 LF 고정 — CRLF면 변수/glob 끝에 \r이 붙어
# codex-gate 트리거가 조용히 무력화된다 (codex-gate.profile 등)
*.profile text eol=lf
